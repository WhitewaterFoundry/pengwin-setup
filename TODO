fcitx (?)
language (?)
maintenance (?)
synaptic (?)

finish show_warning change
make sure all pass $@ to show_warning
write new show_warning strings
check if need to delete go build folders
add apt update after removing any sources
